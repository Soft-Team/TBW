extends ../index.pug

block profile
  block append scripts
    script.
      $(window).on('load',function(){
        $('#imgerror').modal('show');
      });
  #imgerror.modal.fade(tabindex='-1' role='dialog' aria-labelledby='imgerrorTitle' aria-hidden='true')
    .modal-dialog(role='document')
      .modal-content
        .modal-header
          h5#imgerrorTitle.modal-title Submission Failed
          button.close(type='button' data-dismiss='modal' aria-label='Close')
            span(aria-hidden='true') ×
        .modal-body
          p Image Type must be jpeg or png.
        .modal-footer
          button.btn.btn-secondary(type='button', data-dismiss='modal') Close
