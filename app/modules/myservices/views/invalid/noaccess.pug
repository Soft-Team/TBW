extends ../index.pug

block myserv
  block append scripts
    script.
      $(window).on('load',function(){
        $('#noAccess').modal('show');
      });
      
  #noAccess.modal.fade(tabindex='-1' role='dialog' aria-labelledby='noAccessTitle' aria-hidden='true')
    .modal-dialog(role='document')
      .modal-content
        .modal-header
          h5#noAccessTitle.modal-title Page Restricted
          button.close(type='button' data-dismiss='modal' aria-label='Close')
            span(aria-hidden='true') ×
        .modal-body
          p The page cannot be accessed or does not exist.
        .modal-footer
          button.btn.btn-secondary(type='button', data-dismiss='modal') Close
