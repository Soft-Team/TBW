extends ../index.pug

block scheduler
  block append scripts
    script.
      $(window).on('load',function(){
        $('#taken').modal('show');
      });
      
  #taken.modal.fade(tabindex='-1' role='dialog' aria-labelledby='takenTitle' aria-hidden='true')
    .modal-dialog(role='document')
      .modal-content
        .modal-header
          h5#takenTitle.modal-title Invalid Input
          button.close(type='button' data-dismiss='modal' aria-label='Close')
            span(aria-hidden='true') ×
        .modal-body
          p Date Already Added.
        .modal-footer
          button.btn.btn-secondary(type='button', data-dismiss='modal') Close
